/* Testing:
	- constant propagation when one of the variables is an int
	- constant folding
*/
function f(var x) {
	return *x+1;
}
function main() {
	var z = read();
	var b = 0;
	b=f(2);
	let x=10 in let y=20 in (x*y*(*z))+(*b);
}

