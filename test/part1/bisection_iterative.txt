function bisection_iterative(var f, var a, var b, var TOL, var NMAX) { 
	var N=1;
	while(N<=NMAX) {
		var c=(a+b)/2;
		if(f(c) == 0 || (b-a)/2 < TOL) {
			print(c);
			N=NMAX+1;
		} else {
			N=N+1;
			if(f(c)*f(a)=>0) {
				a=c;
			} else {
				b=c;
			}
		}
	}
	print("Method failed.");
}

